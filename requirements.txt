# Voice Bot Dependencies
# =======================

# Core WebSocket library (Exotel voice streaming server)
websockets>=12.0
uvloop>=0.19.0; sys_platform != "win32"

# AI Voice Agent (OpenAI brain + ElevenLabs STT/TTS)
openai>=1.40.0
httpx>=0.27.0
supabase>=2.4.0
python-dotenv>=1.0.0
webrtcvad>=2.0.10
# Optional: only if STT_PROVIDER=deepgram
# deepgram-sdk>=4.0.0

# Lightweight HTTP relay for Exotel status/conversation webhooks -> Lovable
aiohttp>=3.9.0

# Live Dashboard Dependencies
flask>=2.0.0
flask-socketio>=5.0.0
eventlet>=0.33.0

# Optional: For testing and development
pytest>=7.0.0
requests>=2.28.0
